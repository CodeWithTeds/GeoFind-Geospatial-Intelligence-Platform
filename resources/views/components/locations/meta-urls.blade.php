<meta name="calculate-bearing-url" content="{{ route('admin.locations.calculate-bearing') }}">
<meta name="calculate-distance-url" content="{{ route('admin.locations.calculate-distance') }}">
<meta name="calculate-midpoint-url" content="{{ route('admin.locations.calculate-midpoint') }}">
<meta name="calculate-triangle-area-url" content="{{ route('admin.locations.calculate-triangle-area') }}">
<meta name="find-points-in-radius-url" content="{{ route('admin.locations.find-points-in-radius') }}">
<meta name="find-hotels-in-radius-url" content="{{ route('admin.locations.find-hotels-in-radius') }}">
<meta name="find-pois-in-radius-url" content="{{ route('admin.locations.find-pois-in-radius') }}">
<meta name="geofence-visualization-url" content="{{ route('admin.locations.geofence-visualization') }}">
<meta name="to-geohash-url" content="{{ route('admin.locations.to-geohash') }}">
<meta name="calculate-route-url" content="{{ route('admin.locations.calculate-route') }}">
<meta name="calculate-convex-hull-url" content="{{ route('admin.locations.calculate-convex-hull') }}">
<meta name="generate-grid-url" content="{{ route('admin.locations.generate-grid') }}">
<meta name="generate-heatmap-url" content="{{ route('admin.locations.generate-heatmap') }}">
<meta name="find-location-clusters-url" content="{{ route('admin.locations.find-location-clusters') }}">
<meta name="get-address-url-template" content="{{ route('admin.locations.get-address', ['location' => 'LOCATION_ID']) }}"> 